
description = 'artemis-odb-maven-plugin'
dependencies {
    compile group: 'org.apache.maven', name: 'maven-plugin-api', version:'2.0'
  compile project(':artemis-odb-weaver')
  compile project(':artemis-odb-matrix')
    compile group: 'org.sonatype.plexus', name: 'plexus-build-api', version:'0.0.7'
    compile(group: 'org.apache.maven.plugin-tools', name: 'maven-plugin-annotations', version:'3.2') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
